We are considering the following for a CodeBase model object, but it is subject to change. The variables are listed.
CodeBase
    - List<String> branchName;
    - String activeBranch;
    - List<Commit> activeCommits;
    //- int[number of commits] heatAccumulations;
Commit
    - RevCommit (from JGit)
    - Map<String pathName, FileObject>
FileObject <--can be renamed to FileMetricObject
    - File metrics